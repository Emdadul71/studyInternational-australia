import Layout from "@/modules/@common/@layout";
import ContactUs from "@/modules/contact_us";
import PartnerList from "@/modules/partner/list";

const PartnerListPage = () => {
  return (
    <Layout>
      <PartnerList />
    </Layout>
  );
};

export default PartnerListPage;
